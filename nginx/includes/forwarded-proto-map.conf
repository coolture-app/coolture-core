# Preserve TLS scheme from an edge proxy (Cloudflare, Traefik, etc.); nginx alone on :80 sees $scheme=http.
map $http_x_forwarded_proto $forwarded_proto {
    default $http_x_forwarded_proto;
    ''      $scheme;
}
